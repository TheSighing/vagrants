#!/bin/bash
mkdir -p /etc/puppet/modules;

# if [ ! -d /etc/puppet/modules/puppetlabs-apache ]; then
#     puppet module install puppetlabs-apache --version 1.11.0
# fi

# if [ ! -d /etc/puppet/modules/puppetlabs-mysql ]; then
#     puppet module install puppetlabs-mysql --version 3.11.0
# fi
